# Basic RAG - Retrieval-Augmented Generation

Takes any repository, goes through the rust files, generates embeddings from them,
and stores them to a vector database (chroma). Then, it uses the embeddings to answer questions.

Very very much a work in progress, it works as a proof of concept.
